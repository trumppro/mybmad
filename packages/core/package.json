{
  "name": "@spine/core",
  "version": "0.0.1",
  "private": true,
  "description": "Deterministic spine core: lifecycle FSM, claims, gates, evidence, events. No LLM SDK may ever be imported here (see product-roadmap.md §0.1).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
