{
  "name": "@oahs/db",
  "version": "0.0.1",
  "private": true,
  "description": "Postgres persistence for the oahs spine: Drizzle schema + async PgEngine, plus a synchronous facade (synckit worker + PGlite) that runs the UNMODIFIED @oahs/core conformance suite against real Postgres semantics (Phase 1 story 11).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build:worker": "node scripts/build-worker.mjs",
    "pretest": "node scripts/build-worker.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.3.0",
    "@oahs/core": "file:../core",
    "drizzle-orm": "^0.44.0",
    "postgres": "^3.4.5",
    "synckit": "^0.11.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
