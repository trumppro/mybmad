{
  "name": "@oahs/runner",
  "version": "0.0.1",
  "private": true,
  "description": "BYO worker runner library (Phase 1 story 14): claim -> claim-named worktree -> mirror-on-dispatch -> invoke coding agent -> parse HALT -> pinned verification -> push -> submit raw evidence. The runner measures; the core judges (product-roadmap.md §2.3).",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@oahs/contracts": "file:../contracts",
    "@oahs/core": "file:../core",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@oahs/spine-api": "file:../../apps/spine-api",
    "@types/node": "^24.0.0",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
